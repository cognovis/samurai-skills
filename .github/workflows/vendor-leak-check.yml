name: vendor-leak-check

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run vendor leak guard
        run: bash scripts/vendor-leak-guard.sh
